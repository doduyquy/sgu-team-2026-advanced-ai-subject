# Configuration Files

This directory contains configuration definitions and templates for autonomous driving experiments.

## Directory Structure

- `templates/`: Schema templates and example configuration files (`.json`, `.yaml`) for defining experiment parameters, agent hyperparameters, environment settings, and seeds.

## Usage Principles

- Experiments should be parameter-driven to maintain reproducibility.
- Template files define the expected fields and conventions for future benchmark runs.
