# Curated Results

This directory contains curated, committed experiment outcomes, tables, and milestone benchmark summaries.

## Guidelines

- Unlike `runs/` (which holds temporary, verbose, and local execution artifacts and is git-ignored), `results/` is tracked by Git.
- Only finalized summaries, key evaluation metrics tables, and lightweight milestone comparison figures should be committed here.
